# 2026-07-25 Daily Log

## Magic Tarot Store Screenshots - Major Fix

**Problem**: All emoji characters (🌅🎲🤔✨⏳🎮❤️ etc.) were rendering as empty boxes (□) in PIL/Pillow on Windows, causing the store screenshots to show broken icons throughout all 5 screenshots × 2 languages.

**Solution**: Complete rewrite of `scripts/generate-store-screenshots.py`:
- Created 25+ PIL-drawn icon functions (`_draw_star`, `_draw_sun`, `_draw_dice`, `_draw_think_bubble`, `_draw_tarot_card`, etc.)
- Built icon dispatcher system (`ICONS` dict + `draw_icon()` function)
- Replaced ALL Unicode emoji usage with drawn shapes across all 5 screenshot functions
- Icons now render consistently: sun (rays), dice (dots), think bubble (?), sparkle (4-point star), hourglass, yin-yang, heart, gamepad, tarot card, checkmark circle, globe, moon, lock, refresh arrow, bookmark, clipboard, speaker, target, infinity, lightning bolt, robot/AI, color palette, RPS fist

**Files modified**: `scripts/generate-store-screenshots.py` (complete rewrite)

**Output**: 10 PNG files (zh/en × 5 screenshots) in `store-assets/screenshots/{zh,en}/`

## Magic Tarot Store Promo Tiles - Complete Rewrite

**Problem**: Promo images (440×280 small + 1400×560 large) had same emoji □ box issue as screenshots. Additional issues: text misalignment, dice icon needed redesign, lock icon had stray dots, layout overflow.

**Solution**: Complete rewrite of `scripts/generate-store-promo.py`:
- Integrated all PIL drawing functions from screenshots script (sun, think bubble, sparkle, hourglass, heart, gamepad, cards, checkmark, globe, moon, lock)
- **Redesigned dice** (removed) → replaced with tarot cards icon throughout
- **Redesigned lock**: clean padlock with single arc shackle (no stray point artifacts)
- **Fixed banner**: title left-aligned at x=185 to avoid icon overlap
- **Bilingual only**: generates 2 files (not 4 language-separated)

**Files modified**: `scripts/generate-store-promo.py`

**Output**: `promo-large-1400x560.png` + `promo-small-440x280.png` in `store-assets/promo/`

## Magic Tarot Store Screenshots - Round 2 Fixes

**Fixes applied to `scripts/generate-store-screenshots.py`**:
1. **screenshot-1 banner**: Changed from `text_center(draw, (240, 95))` to `text_left(draw, (220, 70))` to fix title overlapping with app icon
2. **screenshot-2**: Removed 3 star rating dots (gold circles) from each spread card
3. **screenshot-3**: Color swatch layout optimized — compact left-aligned layout instead of right-edge
4. **screenshot-4**: Card names now separate zh/en (no more "魔法师 The Magician" in English version)
5. **screenshot-5**: Removed lightning ⚡ icon from header; dice icons → tarot cards throughout

**All changes synced between zh and en versions.**

## Magic Tarot Round 3 Fixes

**Promo script fixes** (`scripts/generate-store-promo.py`):
1. **Banner subtitle方框**: `font_en(19)` → `font_zh(19)` for "每日占卜 · Daily Divination" (font_en doesn't support Chinese)
2. **Features描述文字太小**: `font_zh(10)` → `font_zh(12)` (10px caused □ rendering)
3. **CTA按钮居中**: text_center with proper bbox height calculation
4. **小图图标统一**: ICON_VSZ per-icon size dict (sun=11, think/cards/check=18)

**Screenshot script fixes** (`scripts/generate-store-screenshots.py`):
1. **Screenshot-4塔罗牌美化**: Added gold inner border, corner decorations, icon glow ring, star decorations around card names
2. **Screenshot-5 Experience图标统一**: ICON_VIZ_SIZE dict per icon type for visual uniformity
3. **Screenshot-5 CTA垂直居中**: Proper bbox-based centering calculation
4. **Screenshot-5 labels**: Simplified highlight label structure (removed duplicate conditional)

**Global user rule written**: `~/.codebuddy/rules/pil-emoji-qa-RULE.md`
- PIL emoji禁止规则 + 替代图标函数清单
- 中英文混排字体法则
- 生成后自查清单 (7项)
- 宣传图语言规则
- CTA 按钮居中法则
- 图标视觉大小统一法则
- 塔罗牌卡片渲染规范

## Magic Tarot Round 4 Fixes

**Promo script** (`scripts/generate-store-promo.py`):
1. **小宣传图副标题仍为□□□□**: Line 252 still had `font_en(16)` for "每日占卜 · Daily Divination" → changed to `font_zh(16)`
2. **大宣传图 mockup 图标不统一**: Added `MOCK_ICON_SZ` dict (sun=20, cards=22, think=20) for tool cards in mockup

**Screenshot script** (`scripts/generate-store-screenshots.py`):
1. **RPS 图标像 ◡◡ 笑脸**: `_draw_rock_paper_scissors` drew two overlapping ellipses → redesigned as V-shape scissors gesture with two lines from center
2. **screenshot_1 工具卡片图标大小不一**: Added `TOOL_ICON_SIZE` dict (sun=32, cards=36, think=34)
3. **screenshot_4 卡片中心图标大小不一**: Added `CARD_ICON_SIZE` dict (sparkle=44, yinyang=40, star=42)

**Root cause pattern**: Same bug keeps recurring — `font_en()` used for Chinese text. The global rule already covers this but the small promo line was missed in Round 3.

## Magic Tarot Round 5 Fix

**Screenshot script** (`scripts/generate-store-screenshots.py`):
- **体验优化区域6个图标大小不统一**: `ICON_VIZ_SIZE` 范围 22~30 差距过大 → 全部统一为 26px（speaker/clipboard/bookmark/globe/moon/lock），中英文同步生效
- **选择困难症助手区域内容乱码/不相关**: 
  - 两个"塔罗抽牌 Tarot"重复 + "猜拳 RPS"与项目无关 → 改为真实场景（接受Offer/继续深造/自由职业）
  - 结果文字"布 (包掟石头)"乱码 → 改为塔罗建议风格（"✦ 建议：追随内心，星光指引方向"）
  - 图标从 rps/cards/tarot 改为 sparkle/star/yinyang 全塔罗风格
