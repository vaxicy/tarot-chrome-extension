# 2026-09-01 Magic Tarot Popup Fix (Round 2)

## Round 1 (reverted as `84db418`)

First attempt broke the popup entirely by forcing `min-width: 420px` on the
popup body. The popup went from "narrow + flashing" to "64x100 purple blob
with no text". Root cause for the collapse was actually `:root` typed as
`::root`, plus a UTF-8 BOM at the start of `popup.css`.

The user's screenshot showed the popup reduced to ~64x100 pixels with only
the three tool cards partially visible. Lesson: **never apply
`min-width: <px>` to the popup body in an extension popup**, it conflicts
with Chrome's own width clamping.

## Round 2 (current, committed `9033802`)

Real fixes that don't change popup sizing:
- Fix `::root` -> `:root` in `popup.css` (the actual CSS bug that disabled
  all `--color-*` and `--popup-*` variables).
- Remove the UTF-8 BOM at the top of `popup.css` via a small Python script
  (`fix_root2.py`); it could interfere with `body::before` selectors.
- Hide `.tool-card-icon` (`display: none`) and remove the `<span>` emoji
  elements from `popup.html` so the tool cards stay balanced.
- Enlarge `.tool-card-label` 11->13px and `.tool-card-sub` 9->11px to
  keep them legible without the emoji.

**Verification**: `git diff popup.html popup.css` shows only the safe local
changes; popup body width is unchanged.

## Tooling notes
- PowerShell on this box garbles the `d:\迅雷下载\vibe coding\...` path
  because the shell mangles Chinese characters when piped via the encoding
  layer. Workaround: write the fix script to `C:\Users\16704\AppData\Local\Temp\`
  and run it from the project cwd so Python reads the path directly.
- After every popup change, **reload the extension in
  chrome://extensions** before checking the popup — stale caches otherwise
  show the old broken render.

## Round 3 (committed this session) — TRUE root cause found

User reported popup STILL flickering/jumping after reload, with a screenshot
showing a ~50px-wide vertical strip (CJK text one char per line, h1
ellipsized to "魔…").

Root cause: `@media (max-width: 420px)` block at popup.css ~line 5936
(introduced 2026-05-22 in f90bce7) set
`:root { --popup-width: 100vw; --popup-height: 100vh; }`.

Fatal mechanics:
1. `max-width: 420px` is inclusive → fires at the popup's NORMAL 420px width.
2. Once viewport dips below 420px (first-frame race), 100vw locks body width
   to the too-small viewport → Chrome sizes window to min-content (~50px).
3. Below 420px the query still matches → permanently trapped in the strip.
4. 100vh height feedback loop → continuous reflow = the flickering.

Why "it worked before": racy first-frame sizing usually landed ≥420px where
100vw==420px is harmless. Recent sizing disturbances (viewport meta
reappearing, ::root typo) made the first frame fall below 420 and triggered
the trap.

Fixes applied:
- Deleted the whole `@media (max-width: 420px)` block; left a guard comment.
- (Earlier this session) removed `<meta name="viewport">` from popup.html
  again (it had reappeared); added nowrap+ellipsis to `.welcome-content h1`.
- Verified only remaining vw/vh is `max-height: 80vh` on the hidden modal
  (harmless). Card sizes are JS-inline; no CSS media fallback needed.

Lesson written to global rule:
`chrome-popup-no-viewport-media-query-RULE`.

## Round 4 (committed `796e406`) — second inclusive-boundary trap

User asked the popup's size. Declared 420x600, but ACTUAL was 420x500 because
`@media (max-height: 600px)` (popup.css ~1790) fired (inclusive: viewport is
exactly 600px) and set `--popup-height: 500px`. It converged in one step so it
never flickered — it just silently cost 100px of height. Deleted the block.

Popup sizing truth after Round 4: **420 x 600** is now really in effect.

Remaining media queries (both to be removed):
- `@media (max-width: 420px)` ~1790: INCLUSIVE trap, fires at normal 420px,
  forces result cards to 70x112. Mostly inert because popup.js sets card size
  via inline style (which beats CSS), but it is the same class of landmine.
- `@media (max-height: 500px)` ~1803: now dead code (height is 600).

Base card sizes (for reference when rescaling):
`.result-cards .tarot-card` / `.card-3d-wrap` = 90x140; `.tarot-card` = 80x126.

## Pending: shrink popup by ~1/3 (420x600 -> 280x400), awaiting user's pick

Proposal A (recommended): add `zoom: 0.667` to `body` — one line, perfectly
proportional, zero edits to the ~6200-line popup.css. Chromium-only, which is
fine for a Chrome extension. Watch `position: fixed` (body::before starfield,
modals) since zoom changes fixed-positioning containment.
Proposal B: manual rescale of the two vars + every font/padding/card size —
invasive, will miss spots across 6200 lines, sub-10px CJK text gets ugly.

Either way, delete both remaining media queries as part of the change.

## Round 5 (committed `0ceb602`) — popup shrunk 420x600 -> 300x428

User picked option 1 (zoom + delete media queries + font compensation).

1. `body { zoom: 0.714; }` — 300/420 = 0.714; renders 420x600 as 300x428.
   Layout-level (vector), fonts stay crisp. 6200-line CSS untouched otherwise.
   IMPORTANT: zoom does NOT change the CSS viewport — it stays 420x600, so
   media queries still evaluate against 420x600. That is exactly why the
   `@media (max-width:420px)` trap had to be DELETED, not kept.
2. Deleted both remaining media queries (`max-width:420px`, `max-height:500px`).
   Card sizes are now solely popup.js inline styles.
3. Font compensation (147 decls): 10/11/12/13px -> 14/15/16/17px.
   Keeps 1px steps so hierarchy survives; renders 10.0/10.7/11.4/12.1px.
4. Separately bumped 8 real-content 9px rules -> 14px.
   Left 6-8px decorative diagram labels alone (containers only 36-56px wide;
   enlarging them would overflow).

## Tooling lesson (cost me two wrong conclusions this session)

`search_content` with `outputMode: "count"` returned 0 for `font-size:\s*12px`
and `font-size:\s*9px` when content mode proved there were 44 and 10 matches.
**Do not trust `count` mode in this tool — verify with a Python script or
content mode before drawing conclusions about how many matches exist.**
I also wrongly reported a brace syntax error at popup.css 1801/1823 — those
`}` were correctly closing the @media blocks.

## Pre-existing issue (NOT a regression)

popup.css brace balance is -1 (one extra `}`) — verified identical in HEAD
(git show HEAD:popup.css), and zero new `}` lines added by me. A stray top-level
`}` is ignored by CSS parsers, so it is benign. Left as-is.

## Round 6 (committed `b568e25`) — scheme C responsive polish

User said "继续执行" to finish scheme C (welcome-page slim + responsive).

Cross-checked popup.js card sizing under zoom: `createCardEl(card, …, cardWidth, …)`
uses fixed `cardWidth || 80` inline px (no `offsetWidth`/`getBoundingClientRect`
for layout), `getBoundingClientRect` is only used for preview/tooltip positioning.
So zoom scaling is uniform and safe — no card clipping.

Edits to popup.css:
- `#welcome-page` now `overflow-y:auto` + hidden scrollbar (`scrollbar-width:none`,
  `::-webkit-scrollbar{display:none}`) so the (user-felt "太长") welcome page scrolls
  smoothly instead of relying on the wrapper.
- Compacted header: `.welcome-icon` 42->34px / margin 8->4px; `.welcome-content h1`
  22->20px / margin 6->4px; `.welcome-subtitle` 17->15px / margin 10->8px;
  `@keyframes float` translateY -8->-6px. Tighter first screen at 300px width.

Already applied in prior turns (recap): `.spread-filter-bar` single-line
horizontal scroll (flex-wrap:nowrap; overflow-x:auto; scrollbar hidden;
`.spread-filter-btn{flex-shrink:0}`); popup.js categories default "only first
expanded" (`collapsed = stored===null ? index>0 : stored==='true'`) with
localStorage restore; search-clear restore aligned to same default.

Verified: `node --check popup.js` OK; popup.css braces 921/922 (matches HEAD
baseline, no new stray `}`); all edits present via grep.
Pushed `b568e25`. Reload reminder sent via BurntToast.

Scheme C is COMPLETE. Popup is 300x428, responsive, no flicker traps,
welcome page scrolls, categories collapse-by-default.

## Round 7 (committed `92603bf`) — 命运数字/选择困难症 响应式布局

User refined the earlier "搞响应式布局" request: since "再来一次" button
already exists, only hide the collapsing elements before results — do NOT add
a new "重新选择" button.

Edits:
- `popup.html`: `#numgen-reading` now starts with `hidden` class (was always
  visible). `#numgen-result`/`#numgen-stats`/`#numgen-history` already
  hidden-by-default and shown only after generation — already responsive.
- `popup.js` `generateFateNumber()`: after writing reading content, remove
  `hidden` from `#numgen-reading`. Same reveal when restoring a saved result
  in `showNumberGenPage`.
- `dilemma-spread.js` `resetResultArea()`: re-show `#dilemma-methods`
  (the "或选择具体方式" block).
- `dilemma-spread.js` `showResult()`: hide `#dilemma-methods` once a result
  appears. Existing `#dilemma-redo-btn` ("再来一次") calls
  `resetResultArea()` → `chooseMethod()` which restores the methods block, so
  no new button needed.

Effect: both number-gen and dilemma pages only show result/reading/method
blocks AFTER generation; pre-result state is compact. Click behavior unchanged.

Verified: `node --check popup.js` and `dilemma-spread.js` both pass; no lint
errors. Pushed `92603bf`. Reload reminder sent via BurntToast.

Note (tooling): inline `python -c "..."` with the Chinese project path still
gets GBK-corrupted by the shell even inside subprocess cwd strings — must use
a file launcher (`write_to_file` → ascii path → run) for any git/status cmd
on this project. The `tarot_commit.py`/`tarot_push.py` approach works but the
inline `-c` Chinese path failed with "目录名称无效".

## Round 8 (committed `8afeb84`) — 三项 UI 改动

用户三张截图确认后回 "1" 授权执行：

1. **牌阵悬浮预览显示完整条目**：popup.js 移除 `Math.min(localizedPositions.length, 5)`
   截断与 `.hover-more` "+N" 块，改为循环全部牌位。CSS 优化
   `.spread-hover-preview`（max-width 260→300px、max-height 70vh 可滚动、
   字号统一 13px、title 加金色 text-shadow、pos 加 word-break）。
2. **移除「再来一次」**（与「重新占卜」重复）：dilemma-spread.js
   showResult/chooseMethod/bindDilemmaEvents、popup.js showDilemmaPage、
   popup.html 两个按钮（`#dilemma-reading-redo`、`#dilemma-redo-btn`）、
   popup.css 三组样式、constants.js 中英 `dilemma_redo` key 全清，
   grep 验证 0 残留。结果区后靠「选择决定方式」区块自动恢复重选。
3. **弹窗全部改自定义 CSS**：popup.js 新增 `showConfirm(msg, onConfirm)`
   （`#custom-confirm-overlay`，紫金主题、fadeIn/popIn 动画、遮罩点击关闭），
   替换全部 7 处原生 dialog（copyToClipboard 2×alert、delete-history confirm、
   clearHistory confirm、clearNumgenHistory confirm、numgen 3×alert、
   draw error alert）。CSS 加 `.custom-confirm-*` 五组规则。
   grep 验证无原生 alert(/confirm( 残留。

验证：node --check popup.js / dilemma-spread.js / constants.js 全过；
无 lint 错误。提交 `8afeb84`（valid_utf8=True），未 push（Chrome 扩展规则）。
BurntToast Reload 提醒已发。

Tooling: 中文参数经 PowerShell 传 notify.py 也会 GBK 损坏（引号丢失报
ParserError）→ 通知也必须走 ASCII 文件启动器，脚本内硬编码中文调
notify.py（sys.executable 子进程）。delete_file 工具删不了工作区外文件，
用 Python os.remove。